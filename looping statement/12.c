//12.program of armstrong number in c using a for loop & while loop

#include<stdio.h>
int main()
{
int num, digit=0, rem, power = 0; 
      printf("\nEnter the number = "); 
      scanf("%d", &num);
	 int copy num; 
int copy2 = num; 
while(num!=0)
}
{
         num num / 10;
         digit++;
}
        while(copy!=0)
{
rem copy % 10;power
power + pow(rem, digit);
copy copy / 10;
}
       if (power==copy2)
{
}
       else
{
}
return 0;
printf("\n%d is an Armstrong number", ", copy2);
printf("\n%d is not an Armstrong number", copy2);
